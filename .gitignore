# secrets — never commit real keys
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
build/
dist/

# runtime artifacts
ledger/
*.log
logs/

# Alpaca CLI binary — drop the official release into bin/, never commit it
bin/*.exe
bin/*.zip
bin/alpaca

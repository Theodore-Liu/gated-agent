# Demo video — beat sheet and narration (≤3:00)

> Generated by `python scripts/make_media.py script` from
> `scripts/video_beats.py` — one narration sentence = one visual.
> Every artifact on screen is real: a source file of this repo, the
> 2026-08-31 live round from `logs/daily.log`, a ledger record, or a
> screenshot of the live README / dashboard / review doc. Nothing is
> mocked for the camera. Narration is read verbatim by Kokoro TTS.

## Shot 1 — the one-liner (0:00–0:17)

| t | narration | on screen | lower third |
|---|---|---|---|
| 0:00 | This is Gated Agent: an options paper-trading agent whose every order has to survive its own red team. | screenshot `readme_full.png` (crop @ 60) | every order must survive its own red team |
| 0:07 | The signal is deliberately a textbook toy — Faber's ten-month moving average. | `shot2.txt` → **logs\daily.log — scheduled task GatedAgentDaily · 07:00 PT · 2026-08-31** — highlighted: lines 7–7, lines 11–11 | signal = 10-month moving average · on purpose |
| 0:12 | All the engineering effort lives between that signal and broker submission. | README mermaid diagram (full) | signal → mapper → gates → red team → broker |

> "This is Gated Agent: an options paper-trading agent whose every order has to survive its own red team. The signal is deliberately a textbook toy — Faber's ten-month moving average. All the engineering effort lives between that signal and broker submission."

## Shot 2 — a live run (0:17–1:12)

| t | narration | on screen | lower third |
|---|---|---|---|
| 0:17 | Every day, one pass. | `shot2.txt` → **logs\daily.log — scheduled task GatedAgentDaily · 07:00 PT · 2026-08-31** — highlighted: lines 1–1 | one scheduled run per trading day · 07:00 PT |
| 0:19 | First, it checks the pre-registered exits: take profit, stop loss, and a mandatory time-based exit before expiration week. | `close_rules.txt` → **src/gated_agent/position_manager.py** — highlighted: `R1 time gate`, `R2 take profit`, `R3 stop loss` | exit rules R1–R3 · frozen in config before the contest |
| 0:28 | They are rules, not judgement calls: on September first the stop-loss closed I W M for a loss, and wrote down why. | `position_closed.txt` → **ledger\decisions.jsonl — position_closed** — highlighted: `"pnl"`, `"rule"`, `"why"` | position_closed · R3_stop_loss · pnl −828 |
| 0:36 | Then, per symbol: deterministic code maps the signal to a defined-risk spread. | `shot2.txt` → **logs\daily.log — scheduled task GatedAgentDaily · 07:00 PT · 2026-08-31** — highlighted: lines 7–9 | signal → bull call spread · worst case $678 |
| 0:42 | Deterministic risk gates — arithmetic, no LLM — check the worst-case loss against a five-percent cap, a two-percent daily-loss halt, duplicate orders, direction conflicts, and liquidity. | `gates.txt` → **src/gated_agent/gates.py** — highlighted: lines 4–13 | gates.py — arithmetic that cannot be talked out of |
| 0:55 | The halt is not decorative: on September first it fired, and all five of that day's orders were refused. | `gate_check.txt` → **ledger\decisions.jsonl — gate_check** — highlighted: `"allowed": false`, `daily_loss_halt`, `halt floor` | daily_loss_halt · allowed: false · 5 of 5 orders refused · 2026-09-01 |
| 1:01 | Only after every gate passes does an LLM — connected read-only to Alpaca's MCP server — get the one power it has: veto. | `protocol.txt` → **src/gated_agent/redteam_mcp.py** — highlighted: `An LLM connected`, `context, before each order`, `ONLY power is veto`, `path never lets it` | LLM red team · read-only MCP · its only power is veto |

> "Every day, one pass. First, it checks the pre-registered exits: take profit, stop loss, and a mandatory time-based exit before expiration week. They are rules, not judgement calls: on September first the stop-loss closed I W M for a loss, and wrote down why. Then, per symbol: deterministic code maps the signal to a defined-risk spread. Deterministic risk gates — arithmetic, no LLM — check the worst-case loss against a five-percent cap, a two-percent daily-loss halt, duplicate orders, direction conflicts, and liquidity. The halt is not decorative: on September first it fired, and all five of that day's orders were refused. Only after every gate passes does an LLM — connected read-only to Alpaca's MCP server — get the one power it has: veto."

## Shot 3 — the red team saying no (1:12–1:45)

| t | narration | on screen | lower third |
|---|---|---|---|
| 1:12 | This is a real veto from rehearsal week. | `shot3.txt` → **red-team vetoes — ledger-devtest-20260825-27/decisions.jsonl** — highlighted: lines 1–4 | read-only dump of the rehearsal ledger |
| 1:14 | The spread passed the per-trade loss check at under one percent. | `shot3.txt` → **red-team vetoes — ledger-devtest-20260825-27/decisions.jsonl** — highlighted: lines 21–25 | max_loss_scenario → pass |
| 1:18 | The review vetoed it anyway: it would have added a third correlated bullish spread, reusing a strike already in the book. | `shot3_qqq.txt` → **the QQQ veto — per-order math passed, the book did not** — highlighted: lines 5–8 | verdict: veto — a third correlated bull spread, same 712 strike |
| 1:26 | Portfolio concentration risk that per-trade loss math does not capture. | `shot3_qqq.txt` → **the QQQ veto — per-order math passed, the book did not** — highlighted: lines 11–14 | greeks_exposure → veto · the per-order check cannot see the book |
| 1:31 | The review used account and market data fetched over MCP at decision time. | `shot3_qqq.txt` → **the QQQ veto — per-order math passed, the book did not** — highlighted: lines 10–10 | live greeks + open positions, fetched over Alpaca MCP |
| 1:36 | And the protocol derives the final decision from the recorded verdicts: if the model marks any question as failed, the order is vetoed. | `protocol.txt` → **src/gated_agent/redteam_mcp.py — protocol redteam.v1** — highlighted: `"verdict": "pass" \| "veto"`, `veto if ANY question`, `"veto_reasons"` | redteam.v1 · any failed question ⇒ veto |

> "This is a real veto from rehearsal week. The spread passed the per-trade loss check at under one percent. The review vetoed it anyway: it would have added a third correlated bullish spread, reusing a strike already in the book. Portfolio concentration risk that per-trade loss math does not capture. The review used account and market data fetched over MCP at decision time. And the protocol derives the final decision from the recorded verdicts: if the model marks any question as failed, the order is vetoed."

## Shot 4 — the ledger (1:45–2:17)

| t | narration | on screen | lower third |
|---|---|---|---|
| 1:45 | Every decision lands in an append-only JSON Lines ledger: the signal, the gate results, the red-team transcript, and the order intent. | `shot4.txt` → **ledger\decisions.jsonl — append-only, one line per decision** — highlighted: lines 4–9 | ledger/decisions.jsonl · append-only |
| 1:55 | Submitted orders also record the broker's own receipt, | `shot4_record.txt` → **one order_intent record — with the broker's own receipt** — highlighted: `"broker_receipt"`, `"id"`, `"status"`, `"symbol"`, `"submitted"` | broker_receipt.id — Alpaca's order id, not ours |
| 1:59 | and the ledger is reconciled programmatically against the account each round. | `reconciled.txt` → **ledger\decisions.jsonl — position_reconciled** — highlighted: `"kind"`, `"was"`, `"why"` | position_reconciled · believed book vs. the broker's book |
| 2:04 | Beside it runs a negative control: a seeded random signal through the same pipeline, shadow-only and hard-blocked from ever submitting — a baseline for what the same gates do to random choices. | `shot2.txt` → **logs\daily.log — scheduled task GatedAgentDaily · 07:00 PT · 2026-08-31** — highlighted: lines 10–10, lines 14–14, lines 18–18, lines 22–22, lines 26–26 | negative control · shadow only · never submits |

> "Every decision lands in an append-only JSON Lines ledger: the signal, the gate results, the red-team transcript, and the order intent. Submitted orders also record the broker's own receipt, and the ledger is reconciled programmatically against the account each round. Beside it runs a negative control: a seeded random signal through the same pipeline, shadow-only and hard-blocked from ever submitting — a baseline for what the same gates do to random choices."

## Shot 5 — what judges see (2:17–2:57)

| t | narration | on screen | lower third |
|---|---|---|---|
| 2:17 | This is the public dashboard — the page judges see. It shows the live competition paper account. | screenshot `dashboard_full.png` (crop @ 90) | gated-agent-live.streamlit.app · read-only |
| 2:23 | It started with one hundred thousand dollars, and every position is a defined-risk spread. | screenshot `dashboard_full.png` (crop @ second) | $100,000 start · every position a defined-risk spread |
| 2:29 | Beyond returns, we optimized for something rarer — and this file is the part we are proudest of: two adversarial reviews that broke the agent before the market could, | screenshot `advrev_full.png` (crop @ 0) | docs/ADVERSARIAL-REVIEW.md |
| 2:40 | nineteen defects found by attacking our own system — and then a post-mortem of the two the market found anyway on day three. Every one fixed with a regression test. | screenshot `advrev_market.png` (crop @ 0) | 19 + 2 defects · every one pinned by a test · 227 tests |
| 2:51 | Trust is not the absence of bugs. It is evidence that known failure modes stay fixed. | closing card | — |

> "This is the public dashboard — the page judges see. It shows the live competition paper account. It started with one hundred thousand dollars, and every position is a defined-risk spread. Beyond returns, we optimized for something rarer — and this file is the part we are proudest of: two adversarial reviews that broke the agent before the market could, nineteen defects found by attacking our own system — and then a post-mortem of the two the market found anyway on day three. Every one fixed with a regression test. Trust is not the absence of bugs. It is evidence that known failure modes stay fixed."
